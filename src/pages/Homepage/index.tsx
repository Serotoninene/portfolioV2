import { Hero, RecentProjects, StickyText } from "./components";

export default function Homepage() {
  return (
    <div id="Homepage">
      <Hero />
      <StickyText />
      <RecentProjects />
    </div>
  );
}
